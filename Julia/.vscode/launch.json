
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "julia",
      "request": "launch",
      "name": "Run TinyLanguage tests",
      "program": "${workspaceFolder}/TinyLanguage/test/runtests.jl",
      "cwd": "${workspaceFolder}",
      "juliaEnv": "${workspaceFolder}",
      "env": { "JULIA_PROJECT": "${workspaceFolder}" }
    }
  ]
}
